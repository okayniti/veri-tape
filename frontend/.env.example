# Local dev default. On Vercel, set this in Project Settings -> Environment
# Variables to the deployed backend's Render URL instead, e.g.
# https://veritape-api.onrender.com (no trailing slash).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
