# Pinned to exactly what this repo has been built and tested against, so a
# routine dependency release between now and a Render/Vercel deploy can't
# quietly change behavior or break the build. Bump deliberately, not by
# dropping the pins.
#
# CPU-only PyTorch: the default PyPI wheel bundles CUDA runtime libraries
# nothing here uses (Render's free tier has no GPU), which inflates both the
# build and the loaded process's memory footprint for no benefit. This extra
# index is tried first; if a given torch version isn't published there yet,
# remove the line and pip falls back to the default (larger) PyPI wheel.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy==2.2.3
pandas==2.2.3
scikit-learn==1.8.0
joblib==1.5.3
xgboost==3.2.0
torch==2.11.0
shap==0.51.0
matplotlib==3.10.0
jinja2==3.1.5
google-genai==2.11.0
python-dotenv==1.2.2
streamlit==1.60.0
fastapi==0.135.2
uvicorn==0.42.0
pytest==9.0.2
